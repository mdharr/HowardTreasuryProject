import { CollectionWithStoriesDto } from './collection-with-stories-dto';

describe('CollectionWithStoriesDto', () => {
  it('should create an instance', () => {
    expect(new CollectionWithStoriesDto()).toBeTruthy();
  });
});
